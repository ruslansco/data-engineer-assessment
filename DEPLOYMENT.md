If your solution will not be installable via `pip install -e .` at the top level of the
repo, or will not be runnable via `python -m data_engineer_assessment`,
please give specific instructions here.